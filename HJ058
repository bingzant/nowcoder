#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main () {
    int n, k;
    cin >> n >> k;
    vector<int> nums(n);
    for(int i = 0; i < n; ++i) {
        int tmp;
        cin >> tmp;
        nums[i] = tmp;
    }
    sort(nums.begin(), nums.end());
    for(int i = 0; i < k; ++i) {
        cout << nums[i] << ' ';
    }
    return 0;
}
